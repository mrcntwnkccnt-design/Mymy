# 🚀 Android Messenger - Полное руководство

## Что создано

Полноценное Android приложение-мессенджер с поддержкой:
- ✅ Регистрации и входа через Firebase
- ✅ Уникального ID для каждого пользователя
- ✅ Поиска пользователей по ID
- ✅ Отправки сообщений в реальном времени
- ✅ Панели со списком всех чатов
- ✅ Material Design интерфейса

## 📂 Структура папок

```
/workspaces/Mymy/
├── AndroidMessenger/          # ← ОСНОВНОЙ ПРОЕКТ
│   ├── app/                   # Android приложение
│   ├── build.gradle           # Конфигурация проекта
│   ├── settings.gradle        # Настройки Gradle
│   ├── SETUP_GUIDE.md        # Полное руководство
│   ├── README_PROJECT.md     # Описание проекта
│   ├── build.sh              # Скрипт сборки
│   ├── install.sh            # Скрипт установки
│   └── quickstart.sh          # Быстрый старт
│
└── README_MESSENGER.md        # Описание проекта (русский)
```

## ⚡ Быстрый старт (5 минут)

### Шаг 1: Firebase Setup (ОБЯЗАТЕЛЬНО)

1. Откройте https://console.firebase.google.com
2. Создайте новый проект `android-messenger-app`
3. Включите **Email/Password Authentication**
4. Создайте **Firestore Database** (в режиме test mode для разработки)
5. Скачайте файл `google-services.json` и поместите его в:
   ```
   /workspaces/Mymy/AndroidMessenger/app/google-services.json
   ```

### Шаг 2: Сборка APK

```bash
cd /workspaces/Mymy/AndroidMessenger

# Вариант 1: Использовать скрипт (рекомендуется)
chmod +x build.sh
./build.sh

# Вариант 2: Команда Gradle напрямую
./gradlew clean assembleDebug
```

Результат: `app/build/outputs/apk/debug/app-debug.apk`

### Шаг 3: Установка на устройство

```bash
# Если используете скрипт
chmod +x install.sh
./install.sh

# Или напрямую
adb install -r app/build/outputs/apk/debug/app-debug.apk

# Запуск приложения
adb shell am start -n com.example.androidmessenger/.ui.activity.SplashActivity
```

## 🏗️ Архитектура приложения

### Слои приложения:

1. **Presentation Layer (UI)**
   - Activities: SplashActivity, AuthActivity, MainActivity
   - Fragments: ChatsFragment, SearchFragment, ChatFragment
   - Adapters: ChatsAdapter, MessagesAdapter

2. **Data Layer (Repository)**
   - AuthRepository - управление аутентификацией
   - ChatRepository - управление сообщениями и чатами
   - Models: User, Message, Chat

3. **Backend (Firebase)**
   - Authentication - Firebase Auth
   - Database - Cloud Firestore
   - Storage structure:
     ```
     /users/{userId}           # Профиль пользователя
     /chats/{chatId}           # Информация о чате
     /chats/{chatId}/messages  # Сообщения в чате
     ```

## 🎮 Использование приложения

### 1. Регистрация

```
1. Откройте приложение
2. Нажмите "Create Account"
3. Введите:
   - Email: your-email@example.com
   - Username: Your Name
   - Password: 123456+ (минимум 6 символов)
4. Нажмите "Register"
5. ⚠️ ЗАПОМНИТЕ ВАШ USER ID (отобразится в уведомлении)
```

### 2. Вход

```
1. Введите Email и Password
2. Нажмите "Login"
3. Автоматический вход при следующем открытии
```

### 3. Поиск пользователей

```
1. Перейдите на вкладку "Search" (увеличительное стекло)
2. Введите User ID человека для переписки
3. Нажмите "Search"
4. Нажмите "Start Chat" для начала переписки
```

### 4. Отправка сообщений

```
1. Нажмите на чат в списке
2. Введите сообщение
3. Нажмите "Send"
4. Сообщение отправляется в реальном времени!
```

### 5. Просмотр всех чатов

```
1. Перейдите на вкладку "Chats" (почта)
2. Видите все активные диалоги
3. Последнее сообщение и время отправки
4. Непрочитанные сообщения отмечены значком
```

## 🛠️ Основные команды

```bash
cd /workspaces/Mymy/AndroidMessenger

# Очистить сборку
./gradlew clean

# Собрать debug APK
./gradlew assembleDebug

# Собрать release APK (требует подписи)
./gradlew assembleRelease

# Установить на устройство
adb install -r app/build/outputs/apk/debug/app-debug.apk

# Посмотреть логи приложения
adb logcat | grep androidmessenger

# Удалить приложение
adb uninstall com.example.androidmessenger

# Синхронизировать Gradle
./gradlew sync

# Полная очистка и пересборка
./gradlew clean build
```

## 📱 Тестирование с двумя аккаунтами

### На одном устройстве (разные браузеры/эмуляторы):

```
АККАУНТ 1:
- Email: alice@test.com
- Password: password123
- Username: Alice

АККАУНТ 2:
- Email: bob@test.com
- Password: password123
- Username: Bob

ТЕСТИРОВАНИЕ:
1. Создайте аккаунт Alice (запомните ее User ID)
2. Выйдите из приложения
3. Создайте аккаунт Bob (запомните ее User ID)
4. В Bob: Search → введите Alice User ID → Start Chat
5. Отправьте сообщение от Bob
6. Выйдите и войдите как Alice
7. Видите сообщение от Bob!
8. Ответьте сообщением
```

## 🐛 Решение типичных проблем

### ❌ "google-services.json not found"
```
✓ Решение:
  1. Скачайте файл с Firebase Console
  2. Поместите в: app/google-services.json
  3. Пересоберите: ./gradlew clean build
```

### ❌ "Build failed - Gradle sync failed"
```
✓ Решение:
  1. Очистите кэш: ./gradlew clean
  2. Переустановите зависимости: ./gradlew build --refresh-dependencies
  3. Откройте в Android Studio: File > Sync Now
```

### ❌ "APK not installing"
```
✓ Решение:
  1. Удалите старое приложение: adb uninstall com.example.androidmessenger
  2. Попробуйте снова: adb install app/build/outputs/apk/debug/app-debug.apk
  3. Проверьте место на устройстве
```

### ❌ "Firebase Authentication failed"
```
✓ Решение:
  1. Проверьте google-services.json валидный
  2. В Firebase Console включите Email/Password auth
  3. Проверьте интернет на устройстве
  4. Посмотрите логи: adb logcat | grep Firebase
```

### ❌ "Messages not syncing"
```
✓ Решение:
  1. Проверьте Firestore Rules в Firebase
  2. Убедитесь, что оба пользователя зарегистрированы
  3. Проверьте интернет соединение
  4. Перезагрузите приложение
```

### ❌ "App crashes on startup"
```
✓ Решение:
  1. Посмотрите Logcat: adb logcat
  2. Фильтруйте по приложению: adb logcat | grep androidmessenger
  3. Проверьте ошибки Firebase
  4. Очистите кэш приложения: adb shell pm clear com.example.androidmessenger
```

## 📚 Файлы документации

| Файл | Содержимое |
|------|-----------|
| [SETUP_GUIDE.md](AndroidMessenger/SETUP_GUIDE.md) | ✅ Полное руководство по настройке (40+ страниц) |
| [README_PROJECT.md](AndroidMessenger/README_PROJECT.md) | ✅ Описание архитектуры проекта |
| [README_MESSENGER.md](../README_MESSENGER.md) | ✅ Описание на русском (в папке Mymy) |

## 🔐 Firebase Security Rules

Добавьте эти правила в Firestore для безопасности:

```firestore
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Доступ к профилю пользователя
    match /users/{userId} {
      allow create: if true;
      allow read, write: if request.auth.uid == userId;
    }
    
    // Доступ к чатам
    match /chats/{chatId} {
      allow read, write: if request.auth.uid == resource.data.userId || 
                           request.auth.uid == resource.data.otherUserId;
    }
    
    // Доступ к сообщениям
    match /chats/{chatId}/messages/{messageId} {
      allow create: if request.auth.uid == request.resource.data.senderId;
      allow read, write: if request.auth.uid == resource.data.senderId || 
                           request.auth.uid == resource.data.recipientId;
    }
  }
}
```

## 🎯 Функциональность по компонентам

### 🔐 Аутентификация (AuthActivity)
- Регистрация новых пользователей
- Вход в существующий аккаунт
- Переключение между режимами Login/Register
- Валидация данных (email, пароль 6+ символов)
- Автоматический вход при запуске (SplashActivity)

### 🔍 Поиск (SearchFragment)
- Поиск по уникальному User ID
- Отображение профиля найденного пользователя
- Кнопка для начала переписки

### 💬 Сообщения (ChatFragment + MessagesAdapter)
- Отправка текстовых сообщений
- Получение сообщений в реальном времени
- Визуальное различие (отправленные/полученные)
- Временные метки для каждого сообщения

### 📋 Панель чатов (ChatsFragment + ChatsAdapter)
- Список всех активных чатов
- Последнее сообщение для быстрого предпросмотра
- Время отправки последнего сообщения
- Отсчет непрочитанных сообщений
- Быстрый доступ к беседам

## 💾 База данных (Firestore)

### Коллекция /users
```javascript
{
  userId: "abc123",
  username: "John Doe",
  email: "john@example.com",
  createdAt: 1234567890000,
  lastSeen: 1234567890000
}
```

### Коллекция /chats
```javascript
{
  chatId: "abc123_def456",
  userId: "abc123",
  otherUserId: "def456",
  otherUserName: "Jane Smith",
  lastMessage: "Hi there!",
  lastMessageTime: 1234567890000,
  unreadCount: 2
}
```

### Подколлекция /chats/{chatId}/messages
```javascript
{
  messageId: "msg123",
  senderId: "abc123",
  recipientId: "def456",
  content: "Hello world!",
  timestamp: 1234567890000,
  isRead: true
}
```

## 🎨 Технологии

| Компонент | Технология | Версия |
|-----------|-----------|--------|
| Язык | Kotlin | 1.8.10+ |
| Android | Gradle | 7.4+ |
| SDK | Android SDK | 24+ |
| Аутентификация | Firebase Auth | Latest |
| База данных | Cloud Firestore | Latest |
| UI Toolkit | Material Design | 1.7.0+ |
| Async | Kotlin Coroutines | 1.6.4+ |
| Architecture | MVVM + Repository | Custom |

## ✅ Что работает

- ✅ Регистрация с уникальным ID
- ✅ Вход с email/password
- ✅ Поиск пользователей по ID
- ✅ Отправка сообщений
- ✅ Получение сообщений в реальном времени
- ✅ Список чатов с последним сообщением
- ✅ Material Design интерфейс
- ✅ Автоматический вход при переоткрытии
- ✅ Безопасный выход из аккаунта

## 🚀 Дополнительные возможности (можно добавить)

- 📷 Отправка фотографий
- 🎵 Отправка голосовых сообщений
- 🔔 Push-уведомления о новых сообщениях
- 👥 Групповые чаты
- 🔍 Поиск по истории сообщений
- ✅ Статус "прочитано/не прочитано"
- 🎨 Персонализация профиля
- 🌙 Темный режим
- 🔒 Шифрование сообщений

---

## 📞 Быстрая помощь

```bash
# Все команды выполняются в папке AndroidMessenger:
cd /workspaces/Mymy/AndroidMessenger

# Переоткрыть проект в Android Studio
# File > Open > /workspaces/Mymy/AndroidMessenger

# Очистить все кэши и пересобрать
./gradlew clean build

# Если не работает Gradle
chmod +x gradlew
./gradlew clean build

# Посмотреть все доступные Gradle задачи
./gradlew tasks

# Запустить на эмуляторе
./gradlew installDebug
```

---

**Создано:** 28 января 2026  
**Платформа:** Android 7.0+ (API 24+)  
**Язык:** Kotlin  
**Backend:** Firebase (Auth + Firestore)  
**Статус:** ✅ Готово к использованию
